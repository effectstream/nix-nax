# Midnight network image tags for network-compose.yml — the preview-compat
# matrix. Override per environment (qanet/preprod/mainnet) as needed.
MIDNIGHT_NODE_VERSION=1.0.0
INDEXER_VERSION=4.3.3
PROOF_SERVER_VERSION=8.1.0
