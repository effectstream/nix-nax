node_modules/
# Compact compiler artifacts — regenerated by `bun run compact`.
src/contract/managed*/
# Per-deployment LevelDB stores and deployment-address json.
tictactoe-level-db*
tictactoe-x-*
tictactoe-o-*
tictactoe-relay-db-*
tictactoe.undeployed.json
# Local-stack PIDs + logs (created by `bun run stack:up`).
.stack-pids.json
.stack-logs/
# Webapp build output.
webapp/dist/
# Claude Code launch config (local).
.claude/

# --- Secrets — NEVER commit ---
.env
.env.*
.elevenlabs_key
*.elevenlabs_key
*.key
*.pem

# --- macOS cruft ---
.DS_Store

# --- Explainer-video build: keep scripts (*.py, segments/, subs.srt); drop media + scratch ---
video-build/source.mov
video-build/__pycache__/
video-build/audio/
video-build/el_audio/
video-build/final_audio/
video-build/overlay/
video-build/samples/
video-build/ev/
video-build/*.m4a
video-build/*.mp4
video-build/*.aiff
video-build/*.png
video-build/subs.ass
video-build/cues.json
video-build/filter_complex.txt
